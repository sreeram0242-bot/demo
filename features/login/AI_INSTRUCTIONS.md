# AI Scope & Boundary: Login Feature

> **CRITICAL RULE FOR AI ASSISTANTS:**
> You are assigned to the **`feature/login`** branch and the **`features/login/`** folder.

### Allowed Actions:
- You may ONLY create, read, and modify files within `features/login/`.
- Use the shared design tokens from `../../shared/style.css`.
- Keep this login module self-contained, responsive, and secure.

### Forbidden Actions:
- DO NOT modify files outside `features/login/` (e.g., `dashboard/`, `settings/`, `checkout/`, `index.html`).
- DO NOT edit `main` branch directly.

### Feature Specification:
- Handles user authentication UI (Email/Password, Remember Me, Social login buttons).
- Form validation and login state management.
