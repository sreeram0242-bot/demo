# AI Scope & Boundary: Checkout Feature

> **CRITICAL RULE FOR AI ASSISTANTS:**
> You are assigned to the **`feature/checkout`** branch and the **`features/checkout/`** folder.

### Allowed Actions:
- You may ONLY create, read, and modify files within `features/checkout/`.
- Use the shared design tokens from `../../shared/style.css`.
- Keep checkout UX clear, fast, and responsive.

### Forbidden Actions:
- DO NOT modify files outside `features/checkout/` (e.g., `login/`, `dashboard/`, `settings/`, `index.html`).
- DO NOT edit `main` branch directly.

### Feature Specification:
- E-commerce checkout page with 2 sample products (e.g. Headphones & Leather Watch).
- Shipping address form & payment summary (Subtotal, Shipping, Tax, Total).
- Interactive "Complete Payment" button with confirmation state.
